1. 提权功能。注意到文件、目录的操作会因为没有sudo权限失败。因此，如果报错权限相关，就弹出对话框，让用户输入ubuntu用户+密码提权。
2. ~~将压缩功能单独设置为插件~~（已完成：`plugins/compress` 插件，支持多选文件/文件夹、指定输出文件夹（默认当前文件夹）、压缩前读取/写入权限预检、SSE 进度与取消）
3. ~~file-image-viewer 插件，能够识别当前文件夹下的所有图片，按名称排列，类似上一张/下一张切换~~（已完成）